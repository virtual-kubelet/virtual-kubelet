The Virtual Kubelet is being deployed on your cluster.

To verify that Virtual Kubelet has started, run:

  kubectl --namespace={{ .Release.Namespace }} get pods -l "app={{ template "vk.name" . }}"

{{- if and (not .Values.apiserverCert) (not .Values.apiserverKey) }}

Note: 
TLS key pair not provided for Virtual Kubelet HTTP listener. A key pair was generated for you. This generated key pair is not suitable for production use.

{{- end }}
